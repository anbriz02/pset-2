const length = 48;

const width = 24;

const diameter = 6;

let areaboard = (length*width);

const radius = (diameter/2);

let pi = Math.pin

const areacircle = Math.multiply(pi(Math.pow(radius,2)));

console.log("\nThe surface area of a standard Cornhole board is " + areaboard-areacircle + "square inch(es).\n")
